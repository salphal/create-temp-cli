import { ReactElement } from 'react';
import { useRoutes } from 'react-router-dom';
import ${CompName} from './${fileName}';

const ${CompName}Routes = () => {

  const routes: ReactElement | null = useRoutes([
    {
      path: "/",
      element: <${CompName} />,
    }
  ]);

  return routes;
};

export default ${CompName}Routes;
